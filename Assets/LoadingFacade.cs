using UnityEngine;
using System.Collections;


/// <summary>
/// Criado com a finalidade controlar o carregamento das páginas pois a tela de carregamento deve ser removida 
/// </summary>
public class LoadingFacade : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
